import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {
  UserName:string,
  UserId:string,
  RoomName:string,
  RoomId:string
}

const StartMeeting = ({UserName,UserId,RoomName,RoomId}: Props|any) => {
  return (
    <View>
      
    </View>
  )
}

export default StartMeeting

const styles = StyleSheet.create({})